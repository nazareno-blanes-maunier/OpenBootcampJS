let altura_cm = 170;
console.log(altura_cm);

let altura_m = altura_cm.toFixed(Number) / 100;
console.log(altura_m);

let peso = 69.5;
console.log(peso);

let altura_m_redondeado = Math.round(altura_m);
console.log(altura_m_redondeado);

let peso_redondeado = Math.floor(peso);
console.log(peso_redondeado);

let comparacion = Number.MAX_VALUE === Number.MAX_VALUE + 1;
console.log(comparacion);
